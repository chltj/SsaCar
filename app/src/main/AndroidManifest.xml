<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SsaCar"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NotificationActivity" />
        <activity android:name=".MyPageActivity" />
        <activity android:name=".CallHereActivity" />
        <activity android:name=".PickUpActivity" />
        <activity android:name=".OnewayActivity" />
        <activity android:name=".LongtermActivity" />
        <activity android:name=".CarDetailActivity" />
        <activity android:name=".CustomerCenterActivity"/>
        <activity android:name=".UsageHistoryActivity"/>
        <activity android:name=".PaymentLicenseActivity"/>
        <activity android:name=".CreditActivity"/>
        <activity android:name=".CarListActivity" android:exported="false"/>
        <activity android:name=".TimeSettingActivity" android:exported="false"/>
        <activity android:name=".PaymentActivity"/>


    </application>

</manifest>